{
  "_comment": "VS Code MCP config — for the built-in MCP support in VS Code (settings.json -> 'mcp' key) or the GitHub Copilot Chat extension.",
  "mcp": {
    "servers": {
      "oci-extras-mcp": {
        "type": "stdio",
        "command": "node",
        "args": ["<repo-root>/oci-mcp/oci-extras-mcp/index.js"],
        "env": {
          "OCI_CONFIG_PROFILE": "DEFAULT",
          "OCI_MCP_DEFAULT_DRY_RUN": "true"
        }
      },
      "oci-cloud": {
        "type": "stdio",
        "command": "cmd",
        "args": ["/c", "uvx", "oracle.oci-cloud-mcp-server@latest"],
        "env": { "OCI_CONFIG_PROFILE": "EXAMPLE_READ_ONLY", "FASTMCP_LOG_LEVEL": "ERROR" }
      },
      "oci-compute": {
        "type": "stdio",
        "command": "cmd",
        "args": ["/c", "uvx", "oracle.oci-compute-mcp-server@latest"],
        "env": { "OCI_CONFIG_PROFILE": "DEFAULT", "FASTMCP_LOG_LEVEL": "ERROR" }
      }
    }
  }
}
